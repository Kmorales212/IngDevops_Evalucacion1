name: CI Pipeline

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v3

      - name: Ejecutar tareas de CI
        run: |
          echo "Ejecutando pipeline de integración continua."
          echo "Acción activada correctamente por push a develop o pull request a main."